# 安全策略

## 支持范围

本项目尚未发布稳定版本，安全修复只针对 `main` 分支的最新代码。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security -> Report a vulnerability** 私下提交报告。若仓库尚未启用私密漏洞报告，请先通过维护者提供的私密联系方式沟通，不要创建包含漏洞细节、访问令牌或设备信息的公开 Issue。

报告时可以提供：

- 受影响的提交或版本
- 可复现步骤和预期影响
- 已脱敏的日志或最小示例
- 建议的修复方式（可选）

请勿提供 Home Assistant 长期访问令牌、小米 OAuth 回调参数、真实实体 ID、`home-assistant-config/` 内容或其他账号凭据。

## 凭据意外泄露

如果凭据曾被提交、截图或公开分享：

1. 立即在 Home Assistant 个人资料页撤销长期访问令牌。
2. 创建新令牌并只写入本地 `.env.local`。
3. 若小米 OAuth 信息可能泄露，在 Xiaomi Home 集成中重新授权。
4. 从 Git 历史清除敏感内容；仅新增一次删除提交并不能消除历史记录。

